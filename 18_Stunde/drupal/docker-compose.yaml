version: '3'

services: 
    drupal:
        image: drupal
        ports:
            - "9090:80"
        volumes: 
            - drupal-www:/var/www/html
    postgres:
        image: postgres
        environment: 
            - POSTGRES_PASSWORD=my-secret-pw

volumes:
    drupal-www: